Target platforms
-----------------------------------------------------------
Mozilla Firefox 2+
Microsoft Internet Explorer 6+
Apple Safari 3+
Opera 9.5+
Google Chrome


Bugs
-----------------------------------------------------------
+ Gallery:
   - must forward mouseup from gallery canvas to gallerybrowser (slider)
+ Animations:
   - fix hover on links that got colored


PRIO A (Required):
-----------------------------------------------------------

+ Search:
   - show search term above results
   - show number of results
   - limit (+paginate) results

+ Blogging:
   - integrate galleries into bloging (done, but still need single images).
   - rss (?)
   - trackback (mk)
   - pingback (mk)
	- make "<title>" depend on view

+ Movies:
   - TBD (db / mk)

+ HTML and template behaviour
   - Link relations (http://de.selfhtml.org/html/kopfdaten/beziehungen.htm) (db)
   - add contextual information to title element

+ CSS
   - Basic print styles (db)
   - List and code styles (db)





PRIO B (Future):
-----------------------------------------------------------

+ General:
   - optimization by Caching
   - optimization by HTTP-Compression
   - optimization by source-compression ("spaceless", YUI, some CSS-compression)
     (done, but still need some CSS compression and perhaps spaceless)

+ Gallery:
   - zip download (choice of size, ...)
   - tagging (mk)
   - Administration:
      - allow to select representative thumbnails in the gallery (mk)

+ Blogging:
   - animation
   - validate blog articles' xhtml
   - implement media & link overview at the bottom (?)
   - Tag cloud

+ CSS
   - advanced print styles (and other media?)








Done:
-----------------------------------------------------------

+ Animations:
   - MSIE: fix .statbar > .label when doing tag based animation

+ General
   - implement login/logout (mku)
   - add msie-specific style sheet for small modifications 
     (position: fixed->absolute, opacity->filter...) (mk/db)

+ Blogging + Galleries:
   - fix "published" behavior (mk)
   - implement <gallery id="yxz123" title="yes" description="yes" /> (mk)

+ Gallery:
   - icons (db --> mk)
   - preserve thumb, display, mini sizes in initial data fixture
   - Check when thumbnails are really created
   - implement layout in cleaned up style
   - use fix grid size for grid view

+ CSS
   - add ie6 compatibility (db)
   - add resize scripting (mk)

